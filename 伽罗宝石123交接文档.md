# 伽罗宝石 1 / 2 / 3 交接文档

## 一、玩法概述

三款游戏均为 3×3 老虎机，核心框架相同，区别在于**第四轴（倍数轴）的特殊模式**。

| 游戏 | gameType | MessageType | 核心差异 |
|------|----------|-------------|---------|
| 伽罗宝石1 | 105500 | 0x4E | 倍数轴（NORMAL / JACKPOOL 两种模式） |
| 伽罗宝石2 | 105501 | 0x4F | 在1的基础上新增 WHEEL 模式（轮盘倍数） |
| 伽罗宝石3 | 105502 | 0x50 | 在1的基础上新增图标分裂扩展（splitTimes） |

**通用游戏流程：**

```
客户端连接
  → sessionEnter → createPlayerGameData（异步）
  → reqConfigInfo → enterGame → sendConfigMessage（返回押注表、奖池初始值等）
  → reqStartGame  → playerStartGame → normal() → checkAward()
                 → calTimes() → triggerWinTask → sendStartGameMessage
```

---

## 二、目录结构

三款游戏结构完全对称，以 garaGemstone1 为例：

```
slots/game/
├── garaGemstone1/
│   ├── GaraGemstone1Constant.java          消息号、图标ID、模式常量
│   ├── GaraGemstone1MessageHandler.java    消息路由（reqConfigInfo / reqStartGame / reqPoolInfo）
│   ├── dao/
│   │   ├── GaraGemstone1GameDataDao.java   玩家游戏数据持久化（MySQL）
│   │   └── GaraGemstone1ResultLibDao.java  结果库读取（Redis）
│   ├── data/
│   │   ├── GaraGemstone1AwardLineInfo.java 单条中奖线信息
│   │   ├── GaraGemstone1GameRunInfo.java   单局运行上下文
│   │   ├── GaraGemstone1MultiplyAxisInfo.java 倍数轴单条配置（iconId/times/weight）
│   │   ├── GaraGemstone1PlayerGameData.java   玩家内存数据
│   │   ├── GaraGemstone1PlayerGameDataDTO.java  DB ↔ 内存转换
│   │   ├── GaraGemstone1PlayerGameDataRoomDTO.java  好友房版本
│   │   └── GaraGemstone1ResultLib.java     结果库对象（含 iconArr / jackpotIds 等）
│   ├── manager/
│   │   ├── AbstractGaraGemstone1GameManager.java  核心逻辑（normal / transAwardLinePbInfo）
│   │   ├── GaraGemstone1GameManager.java          普通场实现（@Component）
│   │   ├── GaraGemstone1RoomGameManager.java      好友房实现（getRoomType=SLOTS_TEAM_UP_ROOM）
│   │   ├── GaraGemstone1GenerateManager.java      图标生成 + 中奖计算
│   │   └── GaraGemstone1SendMessageManager.java   组装并发送协议
│   └── pb/
│       ├── ReqGaraGemstone1EnterGame.java
│       ├── ReqGaraGemstone1StartGame.java
│       ├── ReqGaraGemstone1PoolValue.java
│       ├── ResGaraGemstone1EnterGame.java
│       ├── ResGaraGemstone1StartGame.java
│       ├── ResGaraGemstone1PoolValue.java
│       ├── GaraGemstone1PoolInfo.java       奖池展示信息
│       └── GaraGemstone1WinIconInfo.java    中奖线展示信息
├── garaGemstone2/  （结构同上，额外增加 wheel 相关字段）
└── garaGemstone3/  （结构同上，额外增加 expandMultiIcons 相关字段）
```

---

## 三、配置表索引

### 伽罗宝石1

| 配置表 | ID | 说明 |
|--------|----|------|
| SpecialPlay.xlsx | 5055001 | 倍数轴图标配置（格式：`iconId_times_weight\|...`） |
| BaseRoller.xlsx  | 20550014 | 倍数轴滚轴元素列表 |
| BaseInit.xlsx    | —        | `prizePoolIdList` 填奖池ID，触发 JACKPOOL 奖励 |
| PoolCfg.xlsx     | —        | 奖池比例 `truePool`（0-10000，即 0%~100%） |

### 伽罗宝石2（在1的基础上增加）

| 配置表 | ID | 说明 |
|--------|----|------|
| SpecialPlay.xlsx | 5055011 | 倍数轴图标配置 |
| BaseRoller.xlsx  | 20550114 | 倍数轴滚轴 |
| SpecialAuxiliary.xlsx | 30550101 | Wheel模式倍数表，读 `awardTypeC`（格式：`[[倍数,权重,...],...]`） |

### 伽罗宝石3（在1的基础上增加）

| 配置表 | ID | 说明 |
|--------|----|------|
| SpecialPlay.xlsx | 5055023 | 倍数轴图标配置 |
| BaseRoller.xlsx  | 20550214 | 倍数轴滚轴 |
| SpecialPlay.xlsx | 5055022 | 图标分裂个数表（格式：`count_weight\|...`，count=0表示不分裂） |
| SpecialPlay.xlsx | 5055021 | 分裂图标元素表（格式：`elementId_multiplier_weight\|...`） |

---

## 四、特殊模式说明

### 4.1 JACKPOOL 模式（三款游戏共有）

- 触发条件：第四轴中格随机到 `ID_JACKPOOL=9`（times=0）
- 效果：从 Redis 小奖池按 `truePool/10000` 比例抽取奖励，通过 `rewardFromSmallPool2()` 实现
- 结果库标记：`lib.addJackpotId(poolId)` + `lib.addLibType(JACKPOOL=2)`

```
appendMultiplyAxisIcons() → selectedInfo.iconId == ID_JACKPOOL
  → lib.addJackpotId(poolId)
  → lib.addLibType(JACKPOOL)

AbstractGaraGemstoneXGameManager.normal()
  → rewardFromSmallPool2(gameRunInfo, playerGameData, resultLib.getJackpotIds())
```

### 4.2 WHEEL 模式（仅伽罗宝石2）

- 触发条件：结果库 libTypeSet 包含 `WHEEL=3`，第四轴中格为 `ID_WHEEL=16`
- 效果：从 `SpecialAuxiliary 30550101.awardTypeC` 加权随机一个倍数，作为额外奖励叠加到总获奖金币
- 倍数存储：`GaraGemstone2ResultLib.wheelTimes`
- 计算公式：`totalTimes = multiplyAxisTimes × lineTimes + wheelTimes`

### 4.3 图标分裂扩展（仅伽罗宝石3）

- 触发时机：每局 `checkAward()` 中，`calTimes()` **之前**执行 `expandMultiIcons(lib)`
- 流程：
  1. 从 SpecialPlay 5055022 加权随机出本次**分裂个数**（0=不分裂）
  2. 对每个分裂名额：从 SpecialPlay 5055021 随机一个元素（含倍数），在 3×3 棋盘（pos 1-9）中找该元素对应的**原始图标**位置，替换（不能替换 JACKPOOL 位置），最多重试 100 次
  3. 替换完成后，遍历所有中奖线：若中奖线经过被替换的位置，累乘该位置的 `splitTimes`
- 数据结构：`GaraGemstone3AwardLineInfo.splitTimes`（同一线多个分裂图标则累乘）
- 倍数计算：`totalTimes = baseTimes × splitTimes`，所有线 `totalTimes` 之和再 × `multiplyAxisTimes`

**图标分组（原始 → 2倍变体 → 3倍变体）：**

```
ID_J    = {1, 16, 24}    ID_Q    = {2, 17, 25}
ID_K    = {3, 18, 26}    ID_A    = {4, 19, 27}
ID_GREEN= {5, 20, 28}    ID_BLUE = {6, 21, 29}
ID_RED  = {7, 22, 30}    ID_WILD = {8, 23, 31}
```

---

## 五、中奖计算流程

```
checkAward(arr, lib, freeModel)
  ├── appendMultiplyAxisIcons()   生成第四轴图标，写入 lib.iconArr（length+3）
  ├── winLines()                  检查连线中奖 → lib.awardLineInfoList
  ├── assignPattern()             检查指定图案
  ├── fullLine() / fullLineCount()  满线检查
  ├── overallDisperse()           全局散花
  ├── lineDispersionCount()       连线散花
  ├── [仅gara3] expandMultiIcons() 图标分裂，设置 splitTimes / totalTimes
  └── calTimes()
        gara1/2: lib.addTimes(multiplyAxisTimes × Σ(baseTimes))
        gara2:   + wheelTimes
        gara3:   lib.addTimes(multiplyAxisTimes × Σ(baseTimes × splitTimes))
```

---

## 六、协议字段

### ResGaraGemstoneXEnterGame（进入游戏响应）

| 字段 | 类型 | 说明 |
|------|------|------|
| `stakeList` | `List<Long>` | 押注档位列表（allBetScore） |
| `defaultBet` | `long` | 默认押注值 |
| `poolValue` | `long` | 当前奖池显示值 |
| `status` | `int` | 玩家游戏状态（0=正常） |
| `remainFreeCount` | `int` | 剩余免费局数 |
| `poolList` | `List<PoolInfo>` | 奖池展示参数（初始倍数/最大倍数/增长率） |

### ResGaraGemstoneXStartGame（开始游戏响应）

| 字段 | 类型 | 说明 |
|------|------|------|
| `allGold` | `long` | 操作后玩家金币 |
| `allWinGold` | `long` | 本局总获奖金币 |
| `status` | `int` | 当前状态 |
| `remainFreeCount` | `int` | 剩余免费局数 |
| `iconList` | `List<Integer>` | 3×3+4th轴图标数组（index 1起） |
| `bigWinShow` | `int` | 大奖展示ID |
| `rewardPoolValue` | `long` | 本局奖池奖励金额 |
| `level` / `exp` | `int` | 玩家等级/经验 |
| `winIconInfoList` | `List<WinIconInfo>` | 中奖线列表 |
| `multiplyAxisTimes` | `long` | 第四轴倍数 |

**WinIconInfo 字段：**

| 字段 | gara1 | gara2 | gara3 | 说明 |
|------|-------|-------|-------|------|
| `id` | ✓ | ✓ | ✓ | 中奖线ID |
| `iconIndexs` | ✓ | ✓ | ✓ | 中奖图标坐标 |
| `winGold` | ✓ | ✓ | ✓ | 实际获奖金币（gara3含splitTimes） |
| `baseWinGold` | — | — | ✓ | 原始获奖金币（不含splitTimes） |

---

## 七、关键方法索引

| 方法 | 位置 | 说明 |
|------|------|------|
| `specialPlayConfig()` | GenerateManager | 启动时加载 SpecialPlay 配置到内存 |
| `loadMultiplyAxisConfig()` | GenerateManager | 解析 SpecialPlay 倍数轴配置 → `multiplyAxisInfoList` |
| `loadWheelAxisConfig()` | Gara2GenerateManager | 解析 SpecialAuxiliary wheel配置 → `wheelAxisInfoList` |
| `loadExpandConfig()` | Gara3GenerateManager | 解析 SpecialPlay 5055021/5055022 |
| `appendMultiplyAxisIcons()` | GenerateManager | 生成第四轴三格图标，写入 iconArr |
| `randomSelectAxisInfo()` | GenerateManager | 按权重随机倍数轴配置 |
| `randomSelectWheelAxisInfo()` | Gara2GenerateManager | 按权重随机 wheel 倍数 |
| `expandMultiIcons()` | Gara3GenerateManager | 图标分裂主逻辑 |
| `checkAward()` | GenerateManager | 中奖检查总入口 |
| `calTimes()` | GenerateManager | 计算最终倍数写入 lib |
| `normal()` | AbstractGameManager | 普通局主流程（含奖池奖励） |
| `rewardFromSmallPool2()` | AbstractSlotsGameManager | 按奖池ID列表兑现 JACKPOOL 奖励 |
| `transAwardLinePbInfo()` | AbstractGameManager | AwardLineInfo → WinIconInfo 协议转换 |
| `sendConfigMessage()` | SendMessageManager | 发送进入游戏配置响应 |
| `sendStartGameMessage()` | SendMessageManager | 发送开始游戏结果响应 |
| `createPlayerGameData()` | AbstractSlotsGameManager | 玩家首次进入时创建内存数据（异步执行） |
| `getPlayerGameData()` | AbstractSlotsGameManager | 从 gameDataMap 查内存数据 |

---

## 八、常见问题排查

| 现象 | 原因 | 排查方向 |
|------|------|---------|
| startGame 返回 404 | `getPlayerGameData()` 返回 null | 1. 检查 sessionEnter 是否触发；2. 查 createPlayerGameData 是否抛异常（BaseRoomCfg 缺配置会 NPE） |
| enterGame 返回 404 | 同上，createPlayerGameData 未执行 | 同上 |
| 奖池一直返回 0 | prizePoolIdList 未配置 或 poolCfg 为空 | 检查 BaseInit.xlsx 的 prizePoolIdList 字段 |
| jackpotIds 为空 | SpecialPlay 倍数轴配置缺少 times=0 的条目 | SpecialPlay 5055011/5055023 需含 `9_0_权重` 条目；BaseRoller 需含 iconId=9 |
| wheel 倍数为 0 | SpecialAuxiliary 30550101 未配置或 awardTypeC 为空 | 检查配置表，确认 awardTypeC 有数据 |
| 图标分裂不生效 | 5055022 所有条目 count=0；或 100次重试均失败 | 确认配置格式正确；确认棋盘上存在对应原始图标 |
| 两款游戏互相干扰 | AbstractGaraGemstone2GameManager.getGameType() 返回了错误的常量 | 确保每款游戏的 getGameType() 返回各自的 CoreConst.GameType 常量（已修复：GARA_GEMSTONE_1 → GARA_GEMSTONE_2） |

---

## 九、各游戏差异对比

| 维度 | 伽罗宝石1 | 伽罗宝石2 | 伽罗宝石3 |
|------|----------|----------|----------|
| gameType | 105500 | 105501 | 105502 |
| 第四轴模式 | NORMAL / JACKPOOL | NORMAL / JACKPOOL / WHEEL | NORMAL / JACKPOOL |
| 图标分裂 | 无 | 无 | 有（expandMultiIcons） |
| splitTimes | 无 | 无 | 有 |
| totalTimes | 无 | 无 | 有（baseTimes × splitTimes） |
| WinIconInfo.baseWinGold | 无 | 无 | 有 |
| calTimes 公式 | axis × Σ(base) | axis × Σ(base) + wheel | axis × Σ(base × split) |
| 倍数轴 SpecialPlay | 5055001 | 5055011 | 5055023 |
| 倍数轴 BaseRoller | 20550014 | 20550114 | 20550214 |
