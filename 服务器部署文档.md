## 一、准备

### 1.说明

该服务器为分布式集群架构。

在下面 **环境部署** 中，除java环境之外，其他中间件(zookeeper,mongodb,redis,kafka,nginx) **建议**使用云服务器平台托管服务。

如果使用ec2服务器搭建的中间件服务，则最大同时在线人数最好**不要超过 1w**。

如果使用托管服务，则在 **环境部署** 步骤中可跳过 **中间件服务环境**



### 2.规范

#### 1.docker 目录

根目录在 `/data/docker`， 里面有两个子文件夹 `conf` 和 `volum`

​	 `conf` 对应的是`docker` 存储数据的根目录

​	 `volum` 对应的是`docker`容器各自的映射目录



#### 2.游戏目录

根目录在 `/data/game`， 里面创建各自服务的目录

为了游戏能够不停服更新，需要创建两个文件夹。

**例：**

部署`hall`服务时,在 `/data/game` 目录下创建 `hall1`, `hall2` 目录，这两个目录中只有 `application-master.yaml` 里面的`cluster.name`和`cluster.tcpAddress`不一样，其他配置全部相同

如果还要部署更多的`hall`服务，建议部署在不同的`ec2`上，则同样创建项目目录`hall3`,`hall4`......



## 二、配置要求

### 1.部署游戏服务器

```
系统：Amazon Linux 2023

配置: 4核8G，硬盘500G

数量: 3

自定义命名: server1,server2,server3
```



### 2.部署中间件服务器

```
系统：Amazon Linux 2023

配置: 8核16G，硬盘500G

数量: 1

自定义命名: server4
```



## 三、环境部署

### 1.java环境

```shell
#安装命令
sudo dnf install -y java-21-amazon-corretto-devel

#安装结束后，查看版本号
java -version

#创建项目目录
mkdir -p /data/game
```



### 2.中间件服务环境

#### 1.预创建目录

```shell
#查看目录大小
df -h

#这里假设 / 目录有充足的空间 
mkdir -p /data/docker/conf /data/docker/volum
```



#### 2.安装docker环境

##### 1.安装docker

```shell
dnf install docker -y
```



###### 1修改docker配置

```shell
vim /etc/docker/daemon.json

#内容如下 ， "data-root" 是前面创建的目录
{
  "exec-opts": ["native.cgroupdriver=systemd"],
  "features": {
    "buildkit": true
  },
  "data-root": "/data/docker/conf"
}
```



###### 2启动docker

```shell
# 启动并启用Docker
systemctl start docker
# 开机自启
systemctl enable docker
```



##### 2.启动zookeeper容器

###### 1.创建映射目录

```shell
cd /data/docker/volum

mkdir -p zookeeper01/conf zookeeper01/data zookeeper01/logs
```



2.创建zoo.cfg

```
vim /data/docker/volum/zookeeper01/conf/zoo.cfg

#内容如下
dataDir=/data
dataLogDir=/datalog
tickTime=2000
initLimit=5
syncLimit=2
autopurge.snapRetainCount=3
autopurge.purgeInterval=0
maxClientCnxns=60
standaloneEnabled=true
admin.enableServer=true
server.1=localhost:2888:3888;2181
```



###### 2.启动容器

```shell
docker run -d --name zookeeper01 --privileged=true -p 2181:2181 --restart unless-stopped -v /data/docker/volum/zookeeper01/data:/data -v /data/docker/volum/zookeeper01/conf:/conf -v /data/docker/volum/zookeeper01/logs:/datalog zookeeper:3.8.4
```



##### 3.启动mongodb容器

###### 1.创建映射目录

```shell
cd /data/docker/volum

mkdir -p mongo01/data
```



###### 2.启动容器

```shell
docker run -d --name mongo01 -p 27017:27017 -v /data/docker/volum/mongo01/data:/data/db --restart unless-stopped -e MONGO_INITDB_ROOT_USERNAME=admin -e MONGO_INITDB_ROOT_PASSWORD=j12j34g56 mongo
```

**注意：将命令中的 123456 替换为自己的密码**



##### 4.启动redis容器

**单例**

```
docker run -d --name redis01 -p 6379:6379 -v /data/docker/volum/redis01/data:/data --restart unless-stopped redis --requirepass "j12j34g56"
```

**哨兵**

为 **一主一从三哨兵** 模式，分别部署在三台机器上，后续如果查询压力大，可动态扩展从节点，哨兵必须为单数

这两台机器在下文中称为host1,host2,host3



**节点分布**

```
host1 -> 1主节点 + 1哨兵

host2 -> 1从节点 + 1哨兵

host3 -> 1哨兵
```



###### 1.创建映射目录

**host1**

```shell
cd /data/docker/volum

mkdir -p redisMaster01/data
```



**host2**

```
cd /data/docker/volum

mkdir -p redisSlave01/data
```



###### 2.创建配置

**host1,host2**  在 /data/docker/volum/redis*/目录下创建

```shell
vim redis.conf

#内容如下
port 6379
bind 0.0.0.0
appendonly yes
```



**host1,host2,host3**   在 /data/docker/volum/sentinel*/目录下创建

```shell
#在3台机器上/data/docker/volum/sentinel*/目录下创建data目录
mkdir data

#在3台机器上/data/docker/volum/sentinel*/目录下创建sentinel.conf文件
vim sentinel.conf

#内容如下
port 26379
dir /data

# 监控名为 'rMaster' 的主节点，IP 地址为 Host A 的 IP
# 仲裁票数设置为 2：至少 2 个 Sentinel 认为 Master 宕机才触发故障转移
sentinel monitor rMaster 192.168.1.10 6379 2

# Master 的密码
sentinel auth-pass rMaster 123456

sentinel down-after-milliseconds rMaster 5000
sentinel parallel-syncs rMaster 1
sentinel failover-timeout rMaster 60000
```





###### 2.启动容器

**host1** 

```shell
#启动master
# 假设 /data/redis/master/ 存放了配置文件和数据
docker run -d --name redis-master01 \
  --network host \
  -v /data/docker/volum/redisMaster01/redis.conf:/usr/local/etc/redis/redis.conf \
  -v /data/docker/volum/redisMaster01/data:/data \
  --restart unless-stopped \
  redis \
  redis-server /usr/local/etc/redis/redis.conf --requirepass "123456"
  
  
#启动 Sentinel(S1)
docker run -d --name redis-sentinel01 \
  --network host \
  -v  /data/docker/volum/sentinel01/sentinel.conf:/usr/local/etc/redis/sentinel.conf \
  -v  /data/docker/volum/sentinel01/data:/data \
  --restart unless-stopped \
  redis \
  redis-sentinel /usr/local/etc/redis/sentinel.conf
```



**host2**

```shell
docker run -d --name redis-slave-1 \
  --network host \
  -v /data/docker/volum/redisSlave01/redis.conf:/usr/local/etc/redis/redis.conf \
  -v /data/docker/volum/redisSlave01/data:/data \
  --restart unless-stopped \
  redis \
  redis-server /usr/local/etc/redis/redis.conf \
  --requirepass "123456" \
  --replicaof hostA的内网ip 6379 \
  --masterauth "123456"
  
  
#启动 Sentinel(S2)
docker run -d --name redis-sentinel02 \
  --network host \
  -v  /data/docker/volum/sentinel02/sentinel.conf:/usr/local/etc/redis/sentinel.conf \
  -v  /data/docker/volum/sentinel02/data:/data \
  --restart unless-stopped \
  redis \
  redis-sentinel /usr/local/etc/redis/sentinel.conf
```



**host3**

```shell
#启动 Sentinel(S3)
docker run -d --name redis-sentinel03 \
  --network host \
  -v  /data/docker/volum/sentinel03/sentinel.conf:/usr/local/etc/redis/sentinel.conf \
  -v  /data/docker/volum/sentinel03/data:/data \
  --restart unless-stopped \
  redis \
  redis-sentinel /usr/local/etc/redis/sentinel.conf
```





##### 5.启动kafka容器

###### 1.创建映射目录

```shell
cd /data/docker/volum

mkdir -p kafka01/data

chmod -R 777 /data/docker/volum/kafka01/data
```



###### 2.创建docker网络

```shell
docker network create kafka-tier
```



###### 3.启动容器

```shell
docker run -d --name kafka01 --hostname 本机内网IP \
    --network kafka-tier \
    -p 9092:9092 -p 9093:9093 \
    -v /data/docker/volum/kafka01/data:/tmp/kafka-logs \
    -e KAFKA_BROKER_ID=0 \
    -e KAFKA_PROCESS_ROLES=controller,broker \
    -e KAFKA_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093 \
    -e KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT \
    -e KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://本机内网IP:9092 \
    -e KAFKA_CONTROLLER_QUORUM_VOTERS=0@本机内网IP:9093 \
    -e KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER \
    -e KAFKA_NODE_ID=0 \
    apache/kafka:latest
```





##### 6.启动nginx容器

###### 1.申请证书

**注意:可以使用其他方式申请证书，这里用 certbot 举例**

```shell
#安装certbot
yum install certbot python3-certbot-nginx
#申请证书
certbot certonly --standalone -d sub.example.com
```

**将 sub.example.com 改为自己的域名**

然后跟着提示走，最后生成证书



###### 2.创建映射目录

```shell
cd /data/docker/volum

mkdir -p nginx01/conf.d
```



###### 3.创建 nginx.conf 配置

```shell
cd /data/docker/volum/nginx01

vim nginx.conf

#内容如下
```

```shell
user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /run/nginx.pid;

worker_rlimit_nofile 65535;

events {
    worker_connections  2048;
    use epoll;
    multi_accept on;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;

    include /etc/nginx/conf.d/*.conf;
}
```



###### 4.创建 conf.d 目录下的配置

**创建 default.conf 配置**

```shell
vim /data/docker/volum/nginx01/conf.d/default.conf
```

```shell
server {
    listen 443 ssl;
    #这里改为自己的域名
    server_name sub.example.com;

    # 证书路径
    ssl_certificate /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    # SSL安全配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    location / {
    	#这里的ip和端口是 recharge 服务的内网ip和端口(可以先创建，后面修改)
        proxy_pass http://192.168.3.31:9099;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /privacy.html {
	alias /www/public/privacy.html;
    }
}
```



**创建 game.conf 配置**

```shell
vim /data/docker/volum/nginx01/conf.d/game.conf
```

```shell
#这里的ip和端口是 gate 服务的内网ip和端口(可以先创建，后面修改)
#根据实际情况来，有多少个gate，就填写多少
upstream wss_game {
    server 192.168.3.31:8092 weight=1;
    server 192.168.3.32:8092 weight=1;
}

#这里的ip和端口是 account 服务的内网ip和端口(可以先创建，后面修改)
#根据实际情况来，有多少个account，就填写多少
upstream https_account{
   server 192.168.3.31:8091 weight=1;
   server 192.168.3.32:8091 weight=1;

}


map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}


server {
	#这里的端口是http，account服务器账号注册的端口，可以根据实际情况修改
    listen 9001 ssl;
    #这里改为自己的域名
    server_name sub.example.com;

    # 证书路径
    ssl_certificate /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    # SSL安全配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    location / {
        proxy_pass http://https_account;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
    }
    
    location /ngm {
        proxy_redirect off;
        proxy_pass http://gm服务内网ip:端口;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}


server {
	#这里的端口是wss的端口，可以根据实际情况修改
    listen 9002 ssl;
    #这里改为自己的域名
    server_name sub.example.com;

	# 证书路径
	ssl_certificate /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    # SSL安全配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;


    location / {
        proxy_redirect off;
        proxy_pass http://wss_game;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
```



###### 5.启动容器

```shell
docker run -d --name nginx01 \
--network host
-v /data/docker/volum/nginx01/nginx.conf:/etc/nginx/nginx.conf \
-v /data/docker/volum/nginx01/conf.d:/etc/nginx/conf.d \

#这里要替换成上面生成的证书的路径
-v /etc/letsencrypt/live/sub.example.com/fullchain.pem:/etc/nginx/certs/fullchain.pem \ 
-v /etc/letsencrypt/live/sub.example.com/privkey.pem:/etc/nginx/certs/privkey.pem \
nginx
```



6.证书续期



#### 3.环境检查

执行命令

```shell
docker ps
```

观察所有启动的 docker 容器，是否有前面创建的容器(zookeeper01,mongo01,redis01,kafka01,nginx01)没有启动的。

如果有，则执行

```shell
docker logs 容器名
```

查看日志，按照提示解决问题，解决后，执行 

```shell
docker restart 容器名
```



#### 4.数据备份

单独准备一台服务器做为数据备份

##### 1.设置免密ssh

在 **源服务器** 生成新的ssh密钥对(不要使用root账户创建)

```shell
ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519
```



##### 2.将公钥复制到目标服务器（ec2-user）

```
#查看并拷贝公钥内容
cat ~/.ssh/id_ed25519.pub

#登录到目标服务器，并且进入的 ~/.ssh 目录
#将上面拷贝的公钥粘贴到 authorized_keys 文件末尾

在 源服务器上测试命令 ssh ec2-user@目标服务器ip，如果一切配置正常，则会登录到目标服务器
```



##### 3.创建备份目录

在目标服务器创建备份目录

举例：

```shell
mkdir -p /data/backup/redis01
mkdir -p /data/backup/mongo01
```



##### 4.脚本(源服务器)

执行时若遇到权限问题，自行解决

```shell
#!/bin/sh

#通用配置
REMOTE_HOST="172.31.33.78"
DATE=$(date +%Y%m%d_%H%M%S)

# redis需要备份的目录
REDIS_BACKUP_DIR="/data/docker/volum/redis01/data/"
# 远程的备份目标目录
REDIS_REMOTE_DIR="/data/backup/redis01/"

#同步redis数据
RSYNC_REDIS_CMD="rsync -avz --exclude='temp-*' ${REDIS_BACKUP_DIR} ec2-user@${REMOTE_HOST}:${REDIS_REMOTE_DIR}"
# 执行rsync命令，并进行重定向
$RSYNC_REDIS_CMD > /dev/null 2>&1

echo '同步redis结束'

# mongo配置
MONGO_CONTAINER="mongo01"
# 先将备份文件导出到本地的目录
MONGO_BACKUP_DIR="/data/docker/volum/mongo01/backup"
# 远程的备份目标目录
MONGO_REMOTE_DIR="/data/backup/mongo01"
MONGO_PWD="123456"
# 容器内临时存储备份文件
CONTAINR_TMP_BACKUP_NAME="/data/backup/latest_backup"

# 清理容器内旧备份
sudo docker exec ${MONGO_CONTAINER} rm -rf ${CONTAINR_TMP_BACKUP_NAME}/* 2>/dev/null

# 删除宿主机上的旧备份
sudo rm -rf ${MONGO_BACKUP_DIR}/*

# 删除远程服务器上的旧备份
ssh ec2-user@${REMOTE_HOST} "rm -rf ${MONGO_REMOTE_DIR}/*"

# 执行备份
sudo docker exec ${MONGO_CONTAINER} mongodump \
    --host localhost \
    --port 27017 \
    --username admin \
    --password ${MONGO_PWD} \
    --out ${CONTAINR_TMP_BACKUP_NAME} \
    --gzip \
    --quiet

# 复制到宿主机
sudo docker cp ${MONGO_CONTAINER}:${CONTAINR_TMP_BACKUP_NAME} ${MONGO_BACKUP_DIR}/

# 同步到远程服务器
RSYNC_MONGO_CMD="rsync -avz ${MONGO_BACKUP_DIR}/ ec2-user@${REMOTE_HOST}:${MONGO_REMOTE_DIR}/"
# 执行rsync命令，并进行重定向
$RSYNC_MONGO_CMD > /dev/null 2>&1

echo '同步mongodb结束'
```



##### 5.创建定时任务

可参考定时清除日志的脚本，这里不做赘述





## 四、游戏服部署

### 1.服务分配

```
server1: slots,hall,gm
server2: table,poker,gate,account
server3: gate,account,recharge,slots
```



### 2.目录介绍

#### 1.config 目录

该目录存放的是该服务的基础配置，如数据库连接，日志配置等

`application.yaml`    该文件决定本服务使用的是哪个配置文件

```shell
spring.profiles.active:
  master  #表示使用的是application-master配置文件
```

`application-master.yaml` 该文件配置了服务的端口或者数据库连接等等

`logback.xml` 该文件为日志配置文件

`nodeConfig.json` 该文件中可配置节点权重和白名单

```shell
{
  "weight": 1,  #节点权重，值范围 >=0。正常运行时要 >=1。如果为0，则该节点若没有连接时，会自动关闭
  "whiteIpList":["192.168.3.90"], #白名单ip，(在gate,gm,recharge中设置无效)
  "whiteIdList": ["1300001"]  #白名单id，(在gate,hall,gm,recharge中设置无效)
}
```



#### 2.resources目录

在 resources/sample 目录中存放的是游戏运行所需要的 excel 配置



### 3.gate部署

服务器网关，与客户端保持websocket连接

```shell
#创建目录
cd /data/game
mkdir -p gate1/config
```

拷贝对应配置文件到相应目录中,该服务没有excel配置，故不需要`resources`目录，修改`application-master.yaml` 文件

```yaml
logging:
  config: config/logback.xml
  level.*: debug

zookeeper:
  connects: 172.31.43.15:2181  #zookeeper的地址和端口
  marsRoot: kinghush  #在zookeeper的节点根目录，在同一集群中，该根节点必须一致

cluster:
  type: GATE  
  name: GTMASTER1 #节点名称，集群内唯一
  tcpAddress:
    port: 13001  #向集群其他服务暴露的端口，在本机内不冲突即可
    host: 172.31.43.15  #本机内网ip

spring:
  main:
    web-application-type: none

gate:
  wsAddress:
    port: 8092  #websocket 端口，在nginx的conf.d目录下的game.conf配置中的 wss_game， 它填写的ip和端口就是本机的内网ip和这个端口
    host: 0.0.0.0
```





### 4.hall部署

游戏大厅

```shell
#创建目录
cd /data/game
mkdir -p hall1/config hall1/resources/sample
```

拷贝对应配置文件到相应目录中，修改`application-master.yaml` 文件

```yaml
logging:
  config: config/logback.xml
  level.*: debug
zookeeper:
  connects: 172.31.43.15:2181  #zookeeper的地址和端口
  marsRoot: kinghush  #在zookeeper的节点根目录，在同一集群中，该根节点必须一致
cluster:
  type: HALL
  name: HALLMASTER1 #节点名称，集群内唯一
  tcpAddress:
    port: 13006 #向集群其他服务暴露的端口，在本机内不冲突即可
    host: 172.31.31.86 #本机内网ip
  masterSelect: true


spring:
  main:
    web-application-type: none
  data:
    redis:
       host: 172.31.43.15  #redis服务ip
       port: 6379  #redis服务端口
       password: 123456  #redis服务密码
       database: 11

    mongodb:
      uri: mongodb://admin:123456@172.31.43.15:27017/kinghush_game_master?authSource=admin  #mongodb连接，需要替换密码，ip和端口
      auto-index-creation: true
  kafka:
    bootstrap-servers: 172.31.43.15:9092  #kafka服务ip和端口
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: 1 
      enable-idempotence: false
      retries: 5
      properties:
        linger.ms: 20
        batch.size: 131072
        compression.type: gzip
        buffer.memory: 134217728
        max.in.flight.requests.per.connection: 5
        send.buffer.bytes: 524288
        receive.buffer.bytes: 524288
        max.request.size: 4194304
        delivery.timeout.ms: 30000
        request.timeout.ms: 20000
        max.block.ms: 30000

thirdservice:
  google:
    verify_url: https://oauth2.googleapis.com/tokeninfo?id_token=
    client_id: 123456-e7jhpvh8nfca5q7n3po35r7nmq332n.apps.googleusercontent.com  #修改google client id
  facebook:
    user_info_url: https://graph.facebook.com/me?fields=id,name,email&access_token=
    debug_token_url: https://graph.facebook.com/debug_token
    app_id: 64000123456471  #修改facebook app id
    app_secret: ab00001234423f32154b7712  #修改facebook app secret
  apple:
    app_id: 6750123529  #修改apple id
    bundle_id: com.jackpot.slotsgame  #修改bundle id
    jwks_url: https://appleid.apple.com/auth/keys

#这里的s3桶是下载后台上传的excel配置，根据实际情况修改
aws:
  s3:
    bucket-name: amzn-xxx-game-bucket
    region: ap-southeast-1
  credentials:
    access-key: AKIAQxxxxxUGE
    secret-key: CqSupfxxxxxxCQnuMGr+DaMdiZ
```



### 5.account部署

负责账号鉴权，注册

```shell
#创建目录
cd /data/game
mkdir -p account1/config account1/resources/sample
```

拷贝对应配置文件到相应目录中，修改`application-master.yaml` 文件

```shell
logging:
  config: config/logback.xml
  level.*: debug
zookeeper:
  connects: 172.31.43.15:2181  #zookeeper的地址和端口
  marsRoot: kinghush  #在zookeeper的节点根目录，在同一集群中，该根节点必须一致
  
server:
  port: 8091  #对外暴露的端口，在nginx的conf.d目录下的game.conf配置中的 https_account， 它填写的ip和端口就是本机的内网ip和这个端口
 
cluster:
  type: ACCOUNT
  name: ACCOUNTMASTER1 #节点名称，集群内唯一
  tcpAddress:
    port: 13002 #向集群其他服务暴露的端口，在本机内不冲突即可
    host: 172.31.31.84 #本机内网ip
  masterSelect: true


spring:
  data:
    redis:
       host: 172.31.43.15  #redis服务ip
       port: 6379  #redis服务端口
       password: 123456  #redis服务密码
       database: 11

    mongodb:
      uri: mongodb://admin:123456@172.31.43.15:27017/kinghush_game_master?authSource=admin  #mongodb连接，需要替换密码，ip和端口
      auto-index-creation: true
  kafka:
    bootstrap-servers: 172.31.43.15:9092  #kafka服务ip和端口
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: 1 
      enable-idempotence: false
      retries: 5
      properties:
        linger.ms: 20
        batch.size: 131072
        compression.type: gzip
        buffer.memory: 134217728
        max.in.flight.requests.per.connection: 5
        send.buffer.bytes: 524288
        receive.buffer.bytes: 524288
        max.request.size: 4194304
        delivery.timeout.ms: 30000
        request.timeout.ms: 20000
        max.block.ms: 30000

thirdservice:
  google:
    verify_url: https://oauth2.googleapis.com/tokeninfo?id_token=
    client_id: 123456-e7jhpvh8nfca5q7n3po35r7nmq332n.apps.googleusercontent.com  #修改google client id
  facebook:
    user_info_url: https://graph.facebook.com/me?fields=id,name,email&access_token=
    debug_token_url: https://graph.facebook.com/debug_token
    app_id: 64000123456471  #修改facebook app id
    app_secret: ab00001234423f32154b7712  #修改facebook app secret
  apple:
    app_id: 6750123529  #修改apple id
    bundle_id: com.jackpot.slotsgame  #修改bundle id
    jwks_url: https://appleid.apple.com/auth/keys

#这里的s3桶是下载后台上传的excel配置，根据实际情况修改
aws:
  s3:
    bucket-name: amzn-xxx-game-bucket
    region: ap-southeast-1
  credentials:
    access-key: AKIAQxxxxxUGE
    secret-key: CqSupfxxxxxxCQnuMGr+DaMdiZ
```

修改`accountConfig.json` 文件

```json
{
  "playerBeginId": 1300000,  #玩家起始id
  "gameservers": [
    "wss://sub.example.com:9002"  #这里配置在nginx的conf.d目录下的game.conf配置中的 wss_game， 它监听的wss域名和端口就是这里填写的，要一致
  ],
  "resourceurls": [
    "http://res.kinghush.com/"   #资源服地址
  ]
}
```



### 6.slots部署

slots游戏

```shell
#创建目录
cd /data/game
mkdir -p slots1/config slots1/resources/sample
```

拷贝对应配置文件到相应目录中，修改`application-master.yaml` 文件

```yaml
logging:
  config: config/logback.xml
  level.*: debug
zookeeper:
  connects: 172.31.43.15:2181  #zookeeper的地址和端口
  marsRoot: kinghush  #在zookeeper的节点根目录，在同一集群中，该根节点必须一致
cluster:
  type: GAME
  name: GAMESLOTSMASTER1 #节点名称，集群内唯一
  gameMajorTypes: [1]
  tcpAddress:
    port: 13011 #向集群其他服务暴露的端口，在本机内不冲突即可
    host: 172.31.31.82 #本机内网ip
  masterSelect: true


spring:
  main:
    web-application-type: none
  data:
    redis:
       host: 172.31.43.15  #redis服务ip
       port: 6379  #redis服务端口
       password: 123456  #redis服务密码
       database: 11

    mongodb:
      uri: mongodb://admin:123456@172.31.43.15:27017/kinghush_game_master?authSource=admin  #mongodb连接，需要替换密码，ip和端口
      auto-index-creation: true
  kafka:
    bootstrap-servers: 172.31.43.15:9092  #kafka服务ip和端口
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: 1 
      enable-idempotence: false
      retries: 5
      properties:
        linger.ms: 20
        batch.size: 131072
        compression.type: gzip
        buffer.memory: 134217728
        max.in.flight.requests.per.connection: 5
        send.buffer.bytes: 524288
        receive.buffer.bytes: 524288
        max.request.size: 4194304
        delivery.timeout.ms: 30000
        request.timeout.ms: 20000
        max.block.ms: 30000

thirdservice:
  google:
    verify_url: https://oauth2.googleapis.com/tokeninfo?id_token=
    client_id: 123456-e7jhpvh8nfca5q7n3po35r7nmq332n.apps.googleusercontent.com  #修改google client id
  facebook:
    user_info_url: https://graph.facebook.com/me?fields=id,name,email&access_token=
    debug_token_url: https://graph.facebook.com/debug_token
    app_id: 64000123456471  #修改facebook app id
    app_secret: ab00001234423f32154b7712  #修改facebook app secret
  apple:
    app_id: 6750123529  #修改apple id
    bundle_id: com.jackpot.slotsgame  #修改bundle id
    jwks_url: https://appleid.apple.com/auth/keys

#这里的s3桶是下载后台上传的excel配置，根据实际情况修改
aws:
  s3:
    bucket-name: amzn-xxx-game-bucket
    region: ap-southeast-1
  credentials:
    access-key: AKIAQxxxxxUGE
    secret-key: CqSupfxxxxxxCQnuMGr+DaMdiZ
```



### 7.table部署

百人场游戏

```shell
#创建目录
cd /data/game
mkdir -p table1/config table1/resources/sample
```

拷贝对应配置文件到相应目录中，修改`application-master.yaml` 文件

```yaml
logging:
  config: config/logback.xml
  level.*: debug
zookeeper:
  connects: 172.31.43.15:2181  #zookeeper的地址和端口
  marsRoot: kinghush  #在zookeeper的节点根目录，在同一集群中，该根节点必须一致
cluster:
  type: GAME
  name: GAMETABLEMASTER1 #节点名称，集群内唯一
  gameMajorTypes: [2]
  tcpAddress:
    port: 13008 #向集群其他服务暴露的端口，在本机内不冲突即可
    host: 172.31.31.81 #本机内网ip
  masterSelect: true


spring:
  main:
    web-application-type: none
  data:
    redis:
       host: 172.31.43.15  #redis服务ip
       port: 6379  #redis服务端口
       password: 123456  #redis服务密码
       database: 11

    mongodb:
      uri: mongodb://admin:123456@172.31.43.15:27017/kinghush_game_master?authSource=admin  #mongodb连接，需要替换密码，ip和端口
      auto-index-creation: true
  kafka:
    bootstrap-servers: 172.31.43.15:9092  #kafka服务ip和端口
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: 1 
      enable-idempotence: false
      retries: 5
      properties:
        linger.ms: 20
        batch.size: 131072
        compression.type: gzip
        buffer.memory: 134217728
        max.in.flight.requests.per.connection: 5
        send.buffer.bytes: 524288
        receive.buffer.bytes: 524288
        max.request.size: 4194304
        delivery.timeout.ms: 30000
        request.timeout.ms: 20000
        max.block.ms: 30000

thirdservice:
  google:
    verify_url: https://oauth2.googleapis.com/tokeninfo?id_token=
    client_id: 123456-e7jhpvh8nfca5q7n3po35r7nmq332n.apps.googleusercontent.com  #修改google client id
  facebook:
    user_info_url: https://graph.facebook.com/me?fields=id,name,email&access_token=
    debug_token_url: https://graph.facebook.com/debug_token
    app_id: 64000123456471  #修改facebook app id
    app_secret: ab00001234423f32154b7712  #修改facebook app secret
  apple:
    app_id: 6750123529  #修改apple id
    bundle_id: com.jackpot.slotsgame  #修改bundle id
    jwks_url: https://appleid.apple.com/auth/keys

#这里的s3桶是下载后台上传的excel配置，根据实际情况修改
aws:
  s3:
    bucket-name: amzn-xxx-game-bucket
    region: ap-southeast-1
  credentials:
    access-key: AKIAQxxxxxUGE
    secret-key: CqSupfxxxxxxCQnuMGr+DaMdiZ
```



### 8.poker部署

扑克类游戏

```
#创建目录
cd /data/game
mkdir -p poker1/config poker1/resources/sample
```

拷贝对应配置文件到相应目录中，修改`application-master.yaml` 文件

```yaml
logging:
  config: config/logback.xml
  level.*: debug
zookeeper:
  connects: 172.31.43.15:2181  #zookeeper的地址和端口
  marsRoot: kinghush  #在zookeeper的节点根目录，在同一集群中，该根节点必须一致
cluster:
  type: GAME
  name: GAMEPOKERMASTER1 #节点名称，集群内唯一
  gameMajorTypes: [3]
  tcpAddress:
    port: 13009 #向集群其他服务暴露的端口，在本机内不冲突即可
    host: 172.31.31.80 #本机内网ip
  masterSelect: true


spring:
  main:
    web-application-type: none
  data:
    redis:
       host: 172.31.43.15  #redis服务ip
       port: 6379  #redis服务端口
       password: 123456  #redis服务密码
       database: 11

    mongodb:
      uri: mongodb://admin:123456@172.31.43.15:27017/kinghush_game_master?authSource=admin  #mongodb连接，需要替换密码，ip和端口
      auto-index-creation: true
  kafka:
    bootstrap-servers: 172.31.43.15:9092  #kafka服务ip和端口
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: 1 
      enable-idempotence: false
      retries: 5
      properties:
        linger.ms: 20
        batch.size: 131072
        compression.type: gzip
        buffer.memory: 134217728
        max.in.flight.requests.per.connection: 5
        send.buffer.bytes: 524288
        receive.buffer.bytes: 524288
        max.request.size: 4194304
        delivery.timeout.ms: 30000
        request.timeout.ms: 20000
        max.block.ms: 30000

thirdservice:
  google:
    verify_url: https://oauth2.googleapis.com/tokeninfo?id_token=
    client_id: 123456-e7jhpvh8nfca5q7n3po35r7nmq332n.apps.googleusercontent.com  #修改google client id
  facebook:
    user_info_url: https://graph.facebook.com/me?fields=id,name,email&access_token=
    debug_token_url: https://graph.facebook.com/debug_token
    app_id: 64000123456471  #修改facebook app id
    app_secret: ab00001234423f32154b7712  #修改facebook app secret
  apple:
    app_id: 6750123529  #修改apple id
    bundle_id: com.jackpot.slotsgame  #修改bundle id
    jwks_url: https://appleid.apple.com/auth/keys
  sms:
  	#短信服务配置，根据实际情况修改
    send_sms_url: https://api.itniotech.com/sms/sendSms
    app_id: EqxxxxxQXs
    app_key: UoIVxxxxxxccqqmz1G4xL
    app_secret: SRVccccxxxxxxxJ1GZp1HU

#这里的s3桶是下载后台上传的excel配置，根据实际情况修改
aws:
  s3:
    bucket-name: amzn-xxx-game-bucket
    region: ap-southeast-1
  credentials:
    access-key: AKIAQxxxxxUGE
    secret-key: CqSupfxxxxxxCQnuMGr+DaMdiZ
```



### 9.gm部署

与后台通信，接受后台的请求

```shell
#创建目录
cd /data/game
mkdir -p gm/config gm/resources/sample
```

拷贝对应配置文件到相应目录中，修改`application-master.yaml` 文件

```yaml
logging:
  config: config/logback.xml
  level.*: debug
zookeeper:
  connects: 172.31.43.15:2181  #zookeeper的地址和端口
  marsRoot: kinghush  #在zookeeper的节点根目录，在同一集群中，该根节点必须一致
cluster:
  type: GM
  name: GMMASTER #节点名称，集群内唯一
  whiteIpList: ["172.31.16.79"]  #本机内网ip
  tcpAddress:
    port: 13016 #向集群其他服务暴露的端口，在本机内不冲突即可
    host: 172.31.31.79 #本机内网ip
  masterSelect: true


server:
  port: 9003  #对外暴露的端口，即后台通过这个端口发起请求


spring:
  data:
    redis:
       host: 172.31.43.15  #redis服务ip
       port: 6379  #redis服务端口
       password: 123456  #redis服务密码
       database: 11

    mongodb:
      uri: mongodb://admin:123456@172.31.43.15:27017/kinghush_game_master?authSource=admin  #mongodb连接，需要替换密码，ip和端口
      auto-index-creation: true
  kafka:
    bootstrap-servers: 172.31.43.15:9092  #kafka服务ip和端口
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: 1 
      enable-idempotence: false
      retries: 5
      properties:
        linger.ms: 20
        batch.size: 131072
        compression.type: gzip
        buffer.memory: 134217728
        max.in.flight.requests.per.connection: 5
        send.buffer.bytes: 524288
        receive.buffer.bytes: 524288
        max.request.size: 4194304
        delivery.timeout.ms: 30000
        request.timeout.ms: 20000
        max.block.ms: 30000

thirdservice:
  google:
    verify_url: https://oauth2.googleapis.com/tokeninfo?id_token=
    client_id: 123456-e7jhpvh8nfca5q7n3po35r7nmq332n.apps.googleusercontent.com  #修改google client id
  facebook:
    user_info_url: https://graph.facebook.com/me?fields=id,name,email&access_token=
    debug_token_url: https://graph.facebook.com/debug_token
    app_id: 64000123456471  #修改facebook app id
    app_secret: ab00001234423f32154b7712  #修改facebook app secret
  apple:
    app_id: 6750123529  #修改apple id
    bundle_id: com.jackpot.slotsgame  #修改bundle id
    jwks_url: https://appleid.apple.com/auth/keys

#这里的s3桶是下载后台上传的excel配置，根据实际情况修改
aws:
  s3:
    bucket-name: amzn-xxx-game-bucket
    region: ap-southeast-1
  credentials:
    access-key: AKIAQxxxxxUGE
    secret-key: CqSupfxxxxxxCQnuMGr+DaMdiZ
```



### 10.recharge部署

接受充值回调

```
#创建目录
cd /data/game
mkdir -p recharge1/config recharge1/resources/sample
```

拷贝对应配置文件到相应目录中，修改`application-master.yaml` 文件

```yaml
logging:
  config: config/logback.xml
  level.*: debug
zookeeper:
  connects: 172.31.43.15:2181  #zookeeper的地址和端口
  marsRoot: kinghush  #在zookeeper的节点根目录，在同一集群中，该根节点必须一致
cluster:
  type: RECHARGE
  name: RECHARGEMASTER1 #节点名称，集群内唯一
  tcpAddress:
    port: 13013 #向集群其他服务暴露的端口，在本机内不冲突即可
    host: 172.31.31.70 #本机内网ip
  masterSelect: true


server:
  port: 9099  #对外暴露的端口，在nginx的conf.d目录下的default.conf配置中的 server.location.proxy_pass， 它填写的ip和端口就是本机的内网ip和这个端口

spring:
  data:
    redis:
       host: 172.31.43.15  #redis服务ip
       port: 6379  #redis服务端口
       password: 123456  #redis服务密码
       database: 11

    mongodb:
      uri: mongodb://admin:123456@172.31.43.15:27017/kinghush_game_master?authSource=admin  #mongodb连接，需要替换密码，ip和端口
      auto-index-creation: true
  kafka:
    bootstrap-servers: 172.31.43.15:9092  #kafka服务ip和端口
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: 1 
      enable-idempotence: false
      retries: 5
      properties:
        linger.ms: 20
        batch.size: 131072
        compression.type: gzip
        buffer.memory: 134217728
        max.in.flight.requests.per.connection: 5
        send.buffer.bytes: 524288
        receive.buffer.bytes: 524288
        max.request.size: 4194304
        delivery.timeout.ms: 30000
        request.timeout.ms: 20000
        max.block.ms: 30000

thirdservice:
  google:
    verify_url: https://oauth2.googleapis.com/tokeninfo?id_token=
    client_id: 123456-e7jhpvh8nfca5q7n3po35r7nmq332n.apps.googleusercontent.com  #修改google client id
  facebook:
    user_info_url: https://graph.facebook.com/me?fields=id,name,email&access_token=
    debug_token_url: https://graph.facebook.com/debug_token
    app_id: 64000123456471  #修改facebook app id
    app_secret: ab00001234423f32154b7712  #修改facebook app secret
  apple:
    app_id: 6750123529  #修改apple id
    bundle_id: com.jackpot.slotsgame  #修改bundle id
    jwks_url: https://appleid.apple.com/auth/keys

#这里的s3桶是下载后台上传的excel配置，根据实际情况修改
aws:
  s3:
    bucket-name: amzn-xxx-game-bucket
    region: ap-southeast-1
  credentials:
    access-key: AKIAQxxxxxUGE
    secret-key: CqSupfxxxxxxCQnuMGr+DaMdiZ
```



## 五、服务器更新

根据服务种类的来分，更新可以分为3种不同的类型

### 第一种

`gate`、`account` 和 `recharge`  这一类带有http请求，并且由 `nginx` 代理的服务

需要将 nginx 配置中的 `weight` 改为 **0** ，等待没有请求后，直接执行 kill 命令(不能 kill -9) 关闭即可



### 第二种

`hall`、`slots`、`table` 和 `poker` 这一类游戏逻辑服

更新举例：

1.假设当前正在 `hall1` 目录运行大厅服务，并且 `hall2`处于未执行状态

2.可在`hall2`目录更新到最新的代码，(可在`nodeConfig.json`中添加白名单，那么启动后只有在白名单内的ip才会进入到该服务，可以验证功能是否更新)，然后启动，（验证功能后，在后台 **游戏设置 -> 服务器列表**，找到`hall2`编辑，清除白名单即可）

3.通过后台的 **游戏设置 -> 服务器列表**  找到`hall1`编辑，将权重设置为 **0**

4.当`hall1` 连接退出完毕后，`hall1`会自动结束进程





### 第三种

`gm` 服务，可直接 `kill` 进程，更新后重启即可



## 六、问题排查

遇到服务挂掉的情况

#### 1.若该进程不存在

则打包该进程目录下的 `log` 文件夹交由开发人员



#### 2.若该进程存在

​	假设是`hall` 挂掉，或者是因为该进程导致`cpu`或者内存被打满的情况

​	首先找到该进程的`pid` , 假设为 `1234`

​	则执行命令 1

```shell
jstack 1234 > jstack1234.txt  #将1234替换为正确的pid
```

​	继续执行命令2

```shell
jmap -dump:live,format=b,file=jmap1234.hprof 1234   #将1234替换为正确的pid
```

注意：**命令1** 和 **命令2** 会对该进程有性能影响，只能在该服务不可用的情况下使用



然后将上面的 `jstack1234.txt`,`jmap1234.hprof` 和 `log` 文件一起打包交由开发人员



