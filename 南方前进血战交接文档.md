# 南方前进·血战 (ToSouthBlood) 交接文档

## 一、玩法说明

血战是南方前进的升级版，核心规则差异：**打完不等人，一直打到第3名出完为止，最后再统一结算。**

| 节点 | 行为 |
|------|------|
| 第1名出完牌 | 剩余3人立即按手牌赔付第1名；**游戏继续** |
| 第2名出完牌 | 剩余2人立即按手牌赔付第2名；**游戏继续** |
| 第3名出完牌 | 剩余1人立即按手牌赔付第3名；**牌局结束**，发送最终结算通知 |
| 通杀（任意玩家） | 直接完整结算，进入下一把（不走多阶段逻辑） |
| 炸弹 | 打出时立即单独结算，**不计入**最终血战结算汇总 |

---

## 二、目录结构

```
tosouthblood/
├── autohandler/          机器人 & 超时自动出牌
├── cardlib/              权重牌库（控局）
├── constant/             消息号、牌面常量
├── data/
│   ├── ToSouthBloodDataHelper.java
│   └── ToSouthBloodSettlementContext.java   ← 结算上下文（含血战标志）
├── gamephase/
│   ├── ToSouthBloodStartGamePhase.java      发牌、通杀检测
│   ├── ToSouthBloodPlayCardPhase.java        出牌阶段入口
│   └── ToSouthBloodSettlementPhase.java     ← 结算阶段（通杀 / 血战最终两条路）
├── manager/              GM 指令
├── message/
│   ├── bean/
│   │   ├── ToSouthBloodActionInfo.java      ← 新增 isOver / finishRank
│   │   ├── ToSouthBloodCurRoundPlayerInfo.java  ← 新增 isOver / finishRank
│   │   └── ToSouthBloodPlayerSettlementInfo.java ← 新增 rank
│   ├── handler/
│   ├── notify/
│   ├── req/
│   └── resp/
├── room/
│   ├── ToSouthBloodGameController.java      ← 核心逻辑（主要改动在此）
│   └── data/
│       ├── ToSouthBloodGameDataVo.java       ← 新增血战专用字段
│       └── ToSouthBloodGameLog.java
└── util/
    ├── ToSouthBloodCardType.java
    └── ToSouthBloodHandUtils.java
```

---

## 三、游戏状态机

```
WAIT_READY
    ↓ 全员准备
START_GAME（发牌 → 通杀检测）
    ├── 检测到通杀 → SettlementPhase（isInstantWin=true）→ WAIT_READY
    └── 无通杀    → PLAY_CART
                        ↓ 玩家依次出牌
                      [出牌循环]
                        ├── 炸弹打出 → processBombSettlement()（立即结算，不入血战汇总）→ 继续
                        ├── 第1名出完 → processBloodIntermediateSettlement() → 继续（剩3人）
                        ├── 第2名出完 → processBloodIntermediateSettlement() → 继续（剩2人）
                        └── 第3名出完 → processBloodIntermediateSettlement()
                                          → triggerBloodFinalSettlement()
                                          → SettlementPhase（isBloodFinalSettlement=true）
                                          → WAIT_READY
```

---

## 四、核心数据（ToSouthBloodGameDataVo）

### 血战专用字段（每局重置）

| 字段 | 类型 | 说明 |
|------|------|------|
| `finishedPlayerOrder` | `List<Long>` | 按出完顺序记录 playerId，`get(0)` = 第1名 |
| `bloodWinSettlementMap` | `Map<Long, Long>` | 每个玩家在本局血战中间结算的**累计净值**（正=赢，负=输），不含炸弹 |

---

## 五、结算逻辑

### 5.1 中间结算（每次有玩家出完牌时）

`ToSouthBloodGameController.processBloodIntermediateSettlement(winnerId, finishRank)`

```
对所有 isOver=false 且 isDelState=false 的输家：
  cardMulti   = 剩余张数 == 13 ? 张数×2 : 张数
  totalMulti  = cardMulti + 红2数×red2倍 + 黑2数×black2倍 + 最优炸弹倍
  loseScore   = totalMulti × baseBet

  deductItem(输家, loseScore)
  bloodWinSettlementMap[输家] -= loseScore

winScore  = Σ(loseScore) - tax
addItem(赢家, winScore)
bloodWinSettlementMap[赢家] += winScore
```

> **注意**：中间结算只有 1 个赢家，所以输家不乘以 `winners.size()`（与通杀结算不同）。

### 5.2 最终结算通知（第3名出完后）

`ToSouthBloodSettlementPhase.doBloodFinalSettlement()`

- **不再扣/加钱**（已在中间结算完成）
- 读取 `bloodWinSettlementMap` 的累计净值
- 从 `finishedPlayerOrder` 确定名次：`rank = indexOf(playerId) + 1`，未出完的玩家 `rank = 4`
- 发送 `NotifyToSouthBloodSettlementInfo`
- 调用 `dealIncome`、`updatePlayerWinStreak`、`updatePoolBalance`、Kafka 日志

### 5.3 通杀结算（原有逻辑，保持不变）

`ToSouthBloodSettlementPhase.doInstantWinSettlement()`

- 发牌阶段检测通杀 → `context.isInstantWin=true`
- 倍数固定：`13 × 2 = 26`（不计炸弹/2牌）
- 支持多赢家（共同通杀）

---

## 六、协议变更

### 6.1 `NotifyToSouthBloodTurnActionInfo` → `ToSouthBloodActionInfo`

新增字段（出牌后广播给所有人）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `isOver` | `boolean` | 本次出牌的玩家是否已出完全部手牌 |
| `finishRank` | `int` | 出完牌名次（1/2/3），未出完时为 `0` |

触发时机：
- 正常出牌（未出完）：`isOver=false, finishRank=0`
- 血战出完牌（1/2/3名）：`isOver=true, finishRank=N`（先于炸弹/血战结算通知发送）

### 6.2 `ToSouthBloodCurRoundPlayerInfo`（每人轮次状态）

新增字段（每次回合广播都携带）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `isOver` | `boolean` | 该玩家是否已出完所有手牌 |
| `finishRank` | `int` | 出完牌名次（0 表示未出完） |

### 6.3 `ToSouthBloodPlayerSettlementInfo`（结算通知）

新增字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `rank` | `int` | 名次：1=第一名，2=第二名，3=第三名，4=末位 |

通杀结算时：赢家 `rank=1`，输家 `rank=0`（客户端可自行处理）。

---

## 七、关键方法索引

| 方法 | 位置 | 说明 |
|------|------|------|
| `processBloodIntermediateSettlement()` | `ToSouthBloodGameController` | 血战中间结算（赔付+记录） |
| `findNextActivePlayer()` | `ToSouthBloodGameController` | 找下一个未出完的活跃玩家 |
| `triggerBloodFinalSettlement()` | `ToSouthBloodGameController` | 构建血战最终结算上下文并触发结算阶段 |
| `checkNextTurn()` | `ToSouthBloodGameController` | 一轮结束时若赢家已出完，自动找下一领出者 |
| `broadcastLastAction()` | `ToSouthBloodGameController` | 广播含 `isOver`/`finishRank` 的最后出牌通知 |
| `doBloodFinalSettlement()` | `ToSouthBloodSettlementPhase` | 读 bloodMap 构建通知、更新统计 |
| `doInstantWinSettlement()` | `ToSouthBloodSettlementPhase` | 通杀原有逻辑 |
| `addFinishedPlayer()` | `ToSouthBloodGameDataVo` | 记录出完顺序，返回名次 |
| `addBloodSettlement()` | `ToSouthBloodGameDataVo` | 累加血战净值 |

---

## 八、边界情况

| 场景 | 处理方式 |
|------|----------|
| 第1/2名出完后找不到活跃玩家 | `findNextActivePlayer` 返回 null → 直接 `triggerBloodFinalSettlement` |
| 一轮结束，本轮赢家已出完（isOver） | `checkNextTurn` 调用 `findNextActivePlayer` 找下一领出者 |
| 最后一手是炸弹且玩家出完 | 先 `broadcastLastAction`，再 `processBombSettlement`（炸弹结算），再 `processBloodIntermediateSettlement`（血战结算） |
| 资金不足时 | 血战中间结算直接 `deductItem`（不做余额截断），余额不足部分由框架处理 |
| 通杀与血战互斥 | 发牌阶段检测通杀 → `context.isBloodFinalSettlement=false`，走通杀逻辑；无通杀才进出牌阶段 |

---

## 九、与 tosouth（参考实现）的差异

| 维度 | tosouth | tosouthblood |
|------|---------|--------------|
| 结算触发 | 第1名出完即结算 | 第3名出完才最终结算 |
| 中间结算 | 无 | 每名玩家出完立即赔付 |
| 赢家数量 | 1个 | 分阶段（每阶段1个），最终3个赢家1个输家 |
| 结算名次 | 无 | 有（1-4名） |
| 协议字段 | 无出完状态 | `isOver`/`finishRank` |
